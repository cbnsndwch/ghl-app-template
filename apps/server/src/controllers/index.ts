import { SsoSessionController } from './sso-session.controller';
import { OAuthController } from './oauth.controller';

// add controllers you'd like the app to mount here
export const controllers = [SsoSessionController, OAuthController];
